#include <iostream>
#include "myfunctions.hpp"
//include virgolette il compilatore cerca nella cartella corrente se non la trova va nella default
// se triangolari cerca nella cartella di sistema 
int main(){
    int x = 10;
    int y = 20;
    int r;
    int p;
    r = power(x,y);
    p = root(x,y);

}